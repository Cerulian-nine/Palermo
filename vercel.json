{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "NODE_OPTIONS=--openssl-legacy-provider npx expo-cli export:web",
  "outputDirectory": "web-build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
